{
  "name": "geotrust-match",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@stellar/freighter-api": "^1.7.0",
    "@stellar/stellar-sdk": "^11.2.2",
    "buffer": "^6.0.3",
    "mapbox-gl": "^3.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "serve": "^14.2.1"
  },
  "scripts": {
    "start": "set PORT=3366 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "serve -s build -l 8080",
    "azure-start": "npm install -g serve && serve -s build -l 8080"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3"
  }
}
